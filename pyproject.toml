[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dessine"
version = "0.0.0"
description = "DESSiNe: Brian2-based SNN GUI tools"
requires-python = ">=3.10"
dependencies = []